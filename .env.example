# Frontend variables (Vite)
# Point this to your Render/Railway backend URL in production
VITE_API_URL=http://localhost:3001

# Backend variables (Prisma/Express)
# Your Supabase or Neon PostgreSQL connection string
DATABASE_URL="postgresql://user:password@host:port/database"
PORT=3001
